# Matrix Subjects

This folder groups draft matrix subjects that classify which provider-facing
features belong in the required core, optional capability families, or
provider-direct extension lanes.

These subjects stay under `.spec/specs/` because they describe repository-level
contract boundaries for the canonical memory architecture.
